Сайт GitHub Pages перенесён в **корень** репозитория (`index.html`, `styles.css`, `app.js`, `contacts.json`).

В настройках Pages укажите: **Deploy from branch `main`, folder `/` (root)**.

Сборка: `python3 scripts/build_pages_site.py`
